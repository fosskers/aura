GENERAL
-------
When outputing repo dependencies, show what repo they're from?

Move aura to haskell-http.

Factor out the constant calls to `langOf` please!!

Rework `build` to use `catchError`?

RC FILE
-------
When reading the serialized conf file, consider using `readMaybe`.

OPTIONS TO IMPLEMENT
--------------------
- `Ci` `info` -> Gives some stats about the package cache.

COMPLETIONS
-----------
- Fish completions go in /etc/fish/completions
- Bash completions go in /usr/share/bash-completion/completions

OUT OF DATE
-----------
Once dependencies are determined, notify the user if any are O.o.D.
